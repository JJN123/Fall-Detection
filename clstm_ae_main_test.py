from seq_exp import *


if __name__ == "__main__":
        pre_load = None
        
        if pre_load == None:
                print('No model path given, please update pre_load variable in dae_main_test.py')

        else:
                img_width, img_height, win_len = 64,64,8
                hor_flip = True
                dset = 'UR-Filled'
                stride = 1
                img_width, img_height = 64,64

                clstm_ae_exp = SeqExp(dset = dset, pre_load = to_load, img_width = img_width, \
                        img_height = img_height, model = model, model_name = model_name)


                clstm_ae_exp.test(raw = False, animate = True)




























































































































































